package com.demoexample.alogorithmsanddatastructure.alogritms

/*
*1. zip
val listA = listOf("a", "b", "c")
val listB = listOf(1, 2, 3, 4)
val result = listA.zip(listB) { a, b -> "$a$b" }
println(result) // [a1, b2, c3]
*combined the collection elements and check the documention
*
* -----------------------------------------------------
* 2. count
* return elemnts count in collection and also by writting boolean experssion we can get Int reture check the documention
*--------------------------------------------------------
*
* */